<!DOCTYPE html>
<html lang="id">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Mess Karyawan</title>

<!-- Bootstrap -->
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">

<!-- Supabase -->
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2"></script>

<!-- html2canvas -->
<script src="https://cdn.jsdelivr.net/npm/html2canvas@1.4.1/dist/html2canvas.min.js"></script>

<style>
:root{
  --bg: #f1f3f6;
  --card: #ffffff;
  --text: #111827;
  --text-muted: #6b7280;
  --primary: #1e3a8a;
  --border: #ddd;
  --hover: rgba(30,58,138,.05);
  --badge-primary: #0d6efd;
  --badge-secondary: #6c757d;
}

body.dark{
  --bg: #121212;
  --card: #1e1e1e;
  --text: #ffffff;
  --text-muted: #cccccc;
  --primary: #4dabf7;
  --border: #2c2c2c;
  --hover: rgba(77,171,247,0.08);
  --badge-primary: #3b82f6;
  --badge-secondary: #6b7280;
}

body {
  background: var(--bg);
  color: var(--text);
  min-height: 100vh;
  padding: 40px 15px;
  display: flex;
  justify-content: center;
  align-items: flex-start;
  font-family: "Segoe UI", sans-serif;
}

.card {
  max-width: 950px;
  width: 100%;
  background: var(--card);
  border-radius: 16px;
  padding: 25px;
  box-shadow: 0 10px 30px rgba(0,0,0,.15);
  position: relative;
  transition: background 0.3s;
}

h2 { text-align: center; color: var(--primary); margin-bottom: 4px; }
.subtitle { text-align: center; font-size: 14px; color: var(--text-muted); }

.dark-toggle { text-align: right; margin-bottom: 10px; }
.form-control, .btn { height: 45px; border-radius: 10px; transition: all 0.2s ease; }
body.dark .form-control::placeholder { color: var(--text-muted); opacity: 1; }

/* Table styling */
.table-container{
  max-height: 380px;
  overflow: auto;
  border-radius: 10px;
  background: var(--card);
  box-shadow: 0 4px 12px rgba(0,0,0,.1);
}
table { width: 100%; border-collapse: collapse; color: var(--text); }
th {
  position: sticky; top: 0;
  background: var(--primary);
  color: #fff;
}
th, td { border: 1px solid var(--border); padding: 8px; font-size: 14px; transition: all 0.2s ease; }
tbody tr:hover { background: var(--hover); transform: scale(1.01); }
tbody tr:nth-child(even) { background: rgba(0,0,0,0.03); }
body.dark tbody tr:nth-child(even) { background: rgba(255,255,255,0.05); }

.badge.bg-primary { background-color: var(--badge-primary); }
.badge.bg-secondary { background-color: var(--badge-secondary); }

#screenshot-header { display: none; text-align: center; margin-bottom: 10px; }
#screenshot-header img { height: 50px; margin-bottom: 6px; }

.btn-primary {
  background: linear-gradient(135deg,#4dabf7,#1e40af);
  border: none;
}
.btn-outline-primary:hover {
  background: var(--primary);
  color: #fff;
}

td i { margin-right: 4px; }
.footer { text-align: center; font-size: 12px; color: var(--text-muted); margin-top: 20px; }

/* ===== HALF ECLIPSE ICON ONLY ===== */
.eclipse{
  position: relative;
  width: 22px;
  height: 22px;
}
.eclipse .sun{
  position:absolute;
  inset:0;
  border-radius:50%;
  background:#facc15;
  box-shadow:0 0 6px rgba(250,204,21,.8);
}
.eclipse .moon{
  position:absolute;
  inset:0;
  border-radius:50%;
  background:#111;
  transform: translateX(-26px);
  transition: transform .45s ease;
}

/* DARK MODE → GERHANA */
body.dark .eclipse .moon{
  transform: translateX(0);
  box-shadow:
    0 0 6px #22d3ee,
    0 0 14px rgba(34,211,238,.8);
}
</style>
</head>

<body>

<div class="card">

  <!-- DARK MODE (HALF ECLIPSE ICON ONLY) -->
  <div class="dark-toggle">
    <button class="btn btn-outline-primary btn-sm" onclick="toggleDark()" title="Half Eclipse">
      <span class="eclipse">
        <span class="sun"></span>
        <span class="moon"></span>
      </span>
    </button>
  </div>

  <h2>Mess Karyawan</h2>
  <div class="subtitle">Sistem Informasi Penghuni Mess</div>
  <hr>

  <div id="screenshot-header">
    <img src="logo.png" alt="Logo">
    <h5 class="fw-semibold mb-0">Data Mess Karyawan</h5>
    <small id="screenshot-time"></small>
  </div>

  <ul class="nav nav-tabs justify-content-center mb-3">
    <li class="nav-item">
      <button class="nav-link active" data-bs-toggle="tab" data-bs-target="#tab1">NIK / Nama</button>
    </li>
    <li class="nav-item">
      <button class="nav-link" data-bs-toggle="tab" data-bs-target="#tab2">Block / No Kamar</button>
    </li>
  </ul>

  <div class="tab-content">

    <!-- TAB 1 -->
    <div class="tab-pane fade show active" id="tab1">
      <div class="row g-2 mb-3">
        <div class="col-md-9"><input id="cariNik" class="form-control" placeholder="Cari NIK atau Nama"></div>
        <div class="col-md-3 d-grid"><button class="btn btn-primary" onclick="cariNikNama()"><i class="bi bi-search"></i> Cari</button></div>
      </div>
      <div class="d-flex justify-content-end mb-2">
        <button class="btn btn-outline-primary btn-sm" onclick="screenshotTabel()"><i class="bi bi-camera"></i> Screenshot</button>
      </div>
      <div id="screenshot-area">
        <div class="table-container">
          <table>
            <thead>
              <tr>
                <th>NIK</th><th>Nama</th><th>Dept</th><th>Jabatan</th><th>Block</th><th>No Kamar</th>
              </tr>
            </thead>
            <tbody id="hasilNik">
              <tr><td colspan="6" class="text-center text-muted">Cari data terlebih dahulu</td></tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>

    <!-- TAB 2 -->
    <div class="tab-pane fade" id="tab2">
      <div class="row g-2 mb-3">
        <div class="col-md-4"><input id="cariBlock" class="form-control" placeholder="Block"></div>
        <div class="col-md-4"><input id="cariKamar" class="form-control" placeholder="No Kamar"></div>
        <div class="col-md-4 d-grid"><button class="btn btn-primary" onclick="cariBlockKamar()"><i class="bi bi-search"></i> Cari</button></div>
      </div>
      <div class="d-flex justify-content-end mb-2">
        <button class="btn btn-outline-primary btn-sm" onclick="screenshotTabel()"><i class="bi bi-camera"></i> Screenshot</button>
      </div>
      <div id="screenshot-area">
        <div class="table-container">
          <table>
            <thead>
              <tr>
                <th>NIK</th><th>Nama</th><th>Dept</th><th>Jabatan</th><th>Block</th><th>No Kamar</th>
              </tr>
            </thead>
            <tbody id="hasilBlock">
              <tr><td colspan="6" class="text-center text-muted">Cari data terlebih dahulu</td></tr>
            </tbody>
          </table>
        </div>
      </div>
    </div>

  </div>

  <div class="footer">© 2026 PT Karunia Permai Sentosa • Internal System</div>

</div>

<script>
/* DARK MODE */
if(localStorage.getItem("dark")==="on") document.body.classList.add("dark");
function toggleDark(){
  document.body.classList.toggle("dark");
  localStorage.setItem("dark",document.body.classList.contains("dark")?"on":"off");
}

/* SUPABASE */
const supabaseClient = supabase.createClient(
  "https://afyqquoxqbnuaijhyszx.supabase.co",
  "YOUR_ANON_KEY"
);

/* RENDER */
function render(tbody,data){
  if(!data||!data.length){
    tbody.innerHTML=`<tr><td colspan="6" class="text-center text-muted">Data tidak ditemukan</td></tr>`;
    return;
  }
  tbody.innerHTML=data.map(r=>`
    <tr>
      <td><i class="bi bi-person"></i> ${r.nik??"-"}</td>
      <td>${r.nama??"-"}</td>
      <td>${r.department??"-"}</td>
      <td>${r.jabatan??"-"}</td>
      <td><span class="badge bg-primary">${r.block??"-"}</span></td>
      <td><span class="badge bg-secondary">${r.no_kamar??"-"}</span></td>
    </tr>`).join("");
}

/* SEARCH */
async function cariNikNama(){
  const {data} = await supabaseClient.from("Karyawan")
    .select("*")
    .or(`nik.ilike.%${cariNik.value}%,nama.ilike.%${cariNik.value}%`)
    .limit(10);
  render(hasilNik,data);
}

async function cariBlockKamar(){
  let q = supabaseClient.from("Karyawan").select("*").limit(10);
  if(cariBlock.value) q=q.eq("block",cariBlock.value);
  if(cariKamar.value) q=q.eq("no_kamar",cariKamar.value);
  const {data} = await q;
  render(hasilBlock,data);
}
</script>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>

